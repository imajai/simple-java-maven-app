pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'sudo /usr/share/maven compile'
            }
        }
      stage('Package') {
            steps {
                sh 'sudo /usr/share/maven package'
            }
        }
        stage('post') {
            steps {
                echo "final stage"
            }
        }
    }
}
