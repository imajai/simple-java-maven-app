pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'sudo maven compile'
            }
        }
      stage('Package') {
            steps {
                sh 'sudo maven package'
            }
        }
        stage('post') {
            steps {
                echo "final stage"
            }
        }
    }
}
