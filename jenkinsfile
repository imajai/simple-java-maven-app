pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'sudo /opt/maven compile'
            }
        }
      stage('Package') {
            steps {
                sh 'sudo /opt/maven package'
            }
        }
        stage('post') {
            steps {
                echo "final stage"
            }
        }
    }
}
